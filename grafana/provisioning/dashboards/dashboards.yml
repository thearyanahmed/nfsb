apiVersion: 1

providers:
  - name: 'nfsb'
    orgId: 1
    folder: 'NFS Benchmarks'
    folderUid: 'nfsb'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
